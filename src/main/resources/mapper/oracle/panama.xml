<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="net.lunalabs.central.mapper.oracle.PanamaMapper">
	<insert id="save">
		INSERT INTO panama(id, productId, username)
		VALUES(panama_seq.nextval, #{productId},#{username})
	</insert>

	
	<delete id="deleteById">
		DELETE FROM panama WHERE id = #{id}
	</delete>

	<update id="update">
		UPDATE panama SET username=#{username}, productId=#{productId} WHERE
		id = #{id}
	</update>
	
	
	<select id="findAll"
		resultType="net.lunalabs.central.domain.oracle.Panama">
		<![CDATA[
		select * from panama order by id desc 
		]]>
	</select>



	<select id="findById"
		resultType="net.lunalabs.central.domain.oracle.Panama">
		<![CDATA[
		select * from panama where id=#{id} 
		]]>
	</select>



</mapper>